# Browser-visible backend URL.
# Local dev backend defaults to http://localhost:5001.
NEXT_PUBLIC_API_URL=http://localhost:5001

# Server-side backend URL used by the public free-lead preview proxy. Keep this
# pointed at the backend origin in production; do not expose Apollo credentials.
BACKEND_API_URL=http://localhost:5001
